<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Lucinda
  body
    h1 Lucinda
    form(action="/query")
      input(type="text")
      button(type="submit") Senden
    .results
      ul 
        each res in results
          li=res

          