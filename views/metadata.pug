extends common

block contents

  form.pure-form(action="/setmeta" method="post")
    fieldset
      table.pure-table.pure-table-bordered
        thead
          tr
            th Element
            th Wert
        each meta in metadata
          tr 
            td #{meta.key} 
            td.wide 
              input.wide(type="text" name=meta.key value=meta.value)

    input(type="hidden" name="complete", value=complete)
    button.pure-button.pure-button-primary(type="submit") Senden
  a(href="/") Zurück 
